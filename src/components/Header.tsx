import React from 'react';
import logo from '../images/logo.png';

const Header = () => {
    return (
        <header>
            <img src={logo} alt="The empire logo" height="40" />
        </header>
    );
};
export default Header;
